const navElCategory = document.querySelectorAll('li.item');
console.log('Number of categories:', navElCategory.length);

const animalEl = document.querySelectorAll
('.item,elem.firstElementChild,#categories');
console.log('Category: Animals Elements:',animalEl.length);

const prodactEl = categories.querySelectorAll
('.item,elem.secondElementChild,#categories');
console.log('Category: Prodacts Elements:',prodactEl.length);

const technologiesEl = categories.querySelectorAll
('ul,#categories,li.item,elem.lastElementChild');
console.log('Category: Technologies Elements:',technologiesEl.length);

